# Example .env file for the Lighthouse Backend

# The connection string for your PostgreSQL database.
# Replace `user`, `password`, `localhost`, `5432`, and `database_name` with your actual database credentials.
DATABASE_URL=postgres://user:password@localhost:5432/database_name

# A secret key used for signing JWT tokens.
# Generate a secure random string to use as your JWT_SECRET.
# Example command to generate a secure key:
#   openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key

# The expiration time for JWT tokens.
# Specify the duration in a format like "1h" (1 hour), "30m" (30 minutes), or "7d" (7 days).
JWT_EXPIRATION=1h

# The port number for the server. Defaults to 8000 if not set.
PORT=8000
